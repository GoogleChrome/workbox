{
  "name": "sw-cli",
  "version": "0.0.18",
  "description": "A CLI tool to generate a service worker and a file manifest making use of the sw-lib module.",
  "keywords": [
    "sw-fw",
    "service worker",
    "caching",
    "fetch requests",
    "offline",
    "cli"
  ],
  "bin": {
    "sw-cli": "build/bin.js"
  },
  "files": [
    "src",
    "build"
  ],
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "prepublish": "gulp build --project sw-cli"
  },
  "author": "Google's Web DevRel Team",
  "license": "Apache-2.0",
  "repository": "googlechrome/sw-helpers",
  "bugs": "https://github.com/googlechrome/sw-helpers/issues",
  "homepage": "https://github.com/GoogleChrome/sw-helpers/tree/master/packages/sw-cli",
  "dependencies": {
    "chalk": "^1.1.3",
    "inquirer": "^2.0.0",
    "meow": "^3.7.0",
    "sw-build": "^0.0.18",
    "update-notifier": "^1.0.3"
  },
  "devDependencies": {
    "fs-extra": "^2.0.0"
  }
}
