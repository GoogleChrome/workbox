{
  "name": "workbox-cli",
  "version": "0.0.1",
  "description": "A CLI tool to generate a service worker and a file manifest making use of the workbox-sw module.",
  "keywords": [
    "workbox",
    "workboxjs",
    "service worker",
    "caching",
    "fetch requests",
    "offline",
    "cli"
  ],
  "bin": {
    "workbox-cli": "build/bin.js"
  },
  "files": [
    "build"
  ],
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "prepublish": "gulp build --project workbox-cli"
  },
  "author": "Google's Web DevRel Team",
  "license": "Apache-2.0",
  "repository": "googlechrome/sw-helpers",
  "bugs": "https://github.com/googlechrome/sw-helpers/issues",
  "homepage": "https://github.com/GoogleChrome/sw-helpers/tree/master/packages/workbox-cli",
  "dependencies": {
    "chalk": "^1.1.3",
    "fs-extra": "^3.0.1",
    "inquirer": "^2.0.0",
    "meow": "^3.7.0",
    "workbox-build": "^0.0.1",
    "update-notifier": "^1.0.3"
  }
}
