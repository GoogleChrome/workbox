module.exports = {
  // This is a directory that should not be commited
  // to git and will be removed and rebuilt between
  // test runs.
  BUNDLE_BUILD_DIRNAME: 'bundle-build',
  ENVIRONMENTS: [undefined, 'production'],
};
