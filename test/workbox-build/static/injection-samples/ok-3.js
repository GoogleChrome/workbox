/* global workbox */
// Example code above

importScripts('./sample-import.js');

const precache = (input) => {
  // no-op
};

precache([]);

const workboxSW = new WorkboxSW();
/* eslint-disable */
workboxSW.precache(    [    ]    );
/* eslint-enable */
